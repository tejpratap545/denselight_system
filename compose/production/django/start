set -o errexit
set -o pipefail
set -o nounset

export DJANGO_SETTINGS_MODULE=config.settings.production
python manage.py migrate
daphne -e ssl:9001:privateKey=private.key:certKey=certificate.crt config.asgi:application
